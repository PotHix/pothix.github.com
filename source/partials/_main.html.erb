<% page_articles.each do |article| %>
  <article id="post-#{article.id}" class="post-#{article.id} post type-post status-publish format-standard has-post-thumbnail hentry category-wordpress tag-version tag-wordpress-2 mix">

    <header class="entry-header">
      <h2 class="entry-title">
	<%= link_to article.title, article, rel: "bookmark" %>
      </h2>
    </header>

    <div class="entry-meta">
      <span class="posted-on">
	Published at <%= link_to article.date.strftime('%Y/%m/%d'), article, rel: "bookmark" %><time class="updated" datetime="<%= article.date.strftime("%Y-%m-%dT%H:%M:%S") %>"><%= article.date.strftime('%b %e, %Y') %></time>
      </span>
      <span class="cat-links">
	In Category <%= link_to article.data.category, category_path(article.data.category), rel: "category tag" %>
      </span>
      <span class="tags-links">
	Tags: <%= article.tags.map{|tag| link_to(tag, tag_path(tag), rel: "tag")}.join(",") %>
      </span>
    </div>

    <div class="entry-content">
      <p><%= article.summary.gsub(/<\/?[^>]*>/,"") %></p>
    </div>
  </article>
<% end %>

<% if paginate && (prev_page || next_page) %>
  <nav role="navigation" id="nav-below" class="paging-navigation">
    <h1 class="screen-reader-text">Post navigation</h1>

    <div class="nav-previous">
      <a href="http://demo.codestag.com/doctype/blog-entries/page/2/" >
	<span class="fa fa-chevron-left"></span>
      </a>
    </div>

    <div class='pages'>
      <%= link_to('&lt;', prev_page, class: "page-numbers") if prev_page %>
      <%= link_to('&gt;', next_page, class: "page-numbers") if next_page %>
    </div>
  </nav>
<% end %>
